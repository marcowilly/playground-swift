import UIKit

//Swift: Comentários - Variáveis - Constantes

//Variáveis (MULTABLE)

/**
 Variaveis (var) são apelidos para acesso em memória de dados.
 */
var name = "Marco Willy Azevedo Gomes"
var age = 21

/**
 O comando abaixo serve para imprimir no console.
Use print(variavel que será impressa)
 */
print(name)

/**
 No Swift podemos usar caracter especial para criação de dados em memória
 */
var 🐶 = "Perola"
var 👼🏻 = 1
var 👶🏽 = 2
var sum = 👼🏻 + 👶🏽
print("O nome do meu cachorro é", 🐶)

//Constantes (UNMUTABLE)

/**
Constantes (let) são usadas criação de dados em memória que são inalteraveis
 */
let PI = 3.1415926
let MOUNTHS_IN_YEAR = 12
